from setuptools import setup

setup(
    name="fastapi-controller",
    version="0.0.3",
    packages=["fastapi_controller"],
    install_requires=[
        "fastapi-utils>=0.2.1",
        "fastapi"
    ]
)
